# Required for Netlify build (prisma generate) and for API functions at runtime.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Optional — only if you use Gemini features in the frontend build.
GEMINI_API_KEY=""
